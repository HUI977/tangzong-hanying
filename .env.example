# 环境变量模板 —— 复制为 .env.local 后填入自己的值
#   cp .env.example .env.local
#
# 全部为可选：不配置时项目仍可正常运行（使用降级方案），便于快速预览。

# ── MapTiler API key（推荐配置）────────────────────────────────
# 底图服务，免费额度 10 万次请求/月：https://cloud.maptiler.com/account/keys/
# 未配置时回退 MapLibre 官方演示底图（无中文注记，仅供功能预览）。
# ⚠️ 部署时请在该 key 的 Allowed Origins 中填入你的域名，避免额度被盗刷。
# VITE_MAPTILER_KEY=your_maptiler_key_here

# ── Google Analytics 衡量 ID（可选）────────────────────────────
# 形如 G-XXXXXXXXXX。未配置时不加载统计脚本。
# VITE_GA_ID=G-XXXXXXXXXX

# ── 坊卡补图端点（可选，高级）──────────────────────────────────
# 坊图文卡在本地图缺失时可请求该端点按提示词生成插画。
# 需自行准备符合以下约定的服务：GET <endpoint>?prompt=<文本>&image_size=portrait_4_3 → 返回图片。
# 未配置时缺失格直接显示占位图。
# VITE_WARD_GEN_ENDPOINT=
